import os
import csv 


poll_data = os.path.join("Resources", "election_data.csv")

#open csv
with open('PyPoll/Resources/election_data.csv', newline= '') as csvfile:
    csvreader = csv.reader(csvfile,delimiter=',')
    next(csvreader, None)

candidate = []
vote_count = {}
percentage = {}

total_votes = 0





 # cycle through data and append candidate list with candidate
                #0          #1       #2     #3
   candiates = ["Correy", "Khan", "Li", "O'Tooley"]
    For x in "candiate " in range (2)
        print (x)   
   string_candidate.append(1)
 # increases vote counter by 1
vote_count = int(row[0]) + 1 
  # The total number of votes cast

  # A complete list of candidates who received votes

  # The percentage of votes each candidate won
   str(round((value/total_votes)*100,3)) + "%" ("+str(value)+ ")"

  # The total number of votes each candidate won

  # The winner of the election based on popular vote.

  # As an example, your analysis should look similar to the one below:


           










